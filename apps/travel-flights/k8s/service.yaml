apiVersion: v1
kind: Service
metadata:
  name: travel-flights
  namespace: travel-lab
  labels:
    app: travel-flights
spec:
  selector:
    app: travel-flights
  ports:
    - name: http
      port: 80
      targetPort: 9080
  type: ClusterIP
