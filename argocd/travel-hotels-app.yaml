apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: travel-hotels
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/saratrajan/dora-metrics
    targetRevision: feature/microservices
    path: apps/travel-hotels/chart
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: travel-hotels-lab
  syncPolicy:
    syncOptions:
      - CreateNamespace=true