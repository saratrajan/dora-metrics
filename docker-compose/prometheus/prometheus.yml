global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: argocd-mock
    static_configs:
      - targets: ["mock-exporter:8000"]
        labels:
          namespace: argocd
